import { styled } from "baseui"

export const InputStyleOverride = {
    Root: {
        style: () => ({
            background: "#FFFFFF",
            border: "1px solid rgba(0, 0, 0, 0.2)",
            borderRadius: "10px"
        })
    }

} 