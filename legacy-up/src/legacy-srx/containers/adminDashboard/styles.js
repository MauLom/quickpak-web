import { styled } from "baseui"

export const PaginationOverrides = {
    PrevButton:{
        style: () =>({
            zIndex:3
        })
    },
    NextButton:{
        style: () =>({
            zIndex:2
        })
    }
}